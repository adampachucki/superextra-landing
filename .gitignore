node_modules
functions/node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
.firebase

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code
.claude
.mcp.json

# Playwright MCP
.playwright-mcp

# ADK
agent/.venv
.adk
.pytest_cache/
agent/logs
agent/test_replay.json
__pycache__

# Live-test harness artifacts (Phase 0 measurement, E2E smokes). The .py
# harness scripts are versioned; their logs + JSON reports are not.
firestore-debug.log
firebase-debug.log
firebase-debug.*.log
agent/tests/phase0_*.log
agent/tests/phase0_*.json
agent/tests/e2e_worker_live*.log
agent/tests/e2e_worker_live*.json

# Research-depth eval harness diagnostic dirs. The .py harness scripts,
# fixtures, instruction variants, scored CSVs, and pairwise verdict JSONs
# ARE versioned (the audit trail). Raw per-run JSONs (~11M) and runner logs
# can be regenerated by re-running the matrix.
agent/evals/results/
agent/evals/logs/

# One-shot legacy backups (kept outside git; regenerate via gcloud if needed)
legacy-sessions-backup.json
